import { Viewport } from "next";

// Define the viewport configuration to be used across the application
export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1.0,
  maximumScale: 1.0,
  minimumScale: 1.0,
};
