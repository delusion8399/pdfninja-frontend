import { Metadata } from "next";
import { viewport } from "../viewport";

export const metadata: Metadata = {
  title: "Add Page Numbers to PDF - PDFNinja",
  description:
    "Add customizable page numbers to your PDF documents with various styles, positions, and formatting options.",
  openGraph: {
    images: ["/page-numbers-og-image.svg"],
  },
};

export { viewport };
